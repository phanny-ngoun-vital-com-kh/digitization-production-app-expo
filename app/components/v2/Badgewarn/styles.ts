import { StyleSheet } from "react-native"

export default StyleSheet.create({
  machinePanel: {
    width: 10,
    height: 10,
    borderRadius: 100,
    marginRight: 5,
    alignItems: "center",
  },
})
